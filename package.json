{
  "name": "encrypted-db",
  "version": "0.0.4",
  "description": "Encrypted IndexedDB wrapper",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": "http://github.com/carathorys/encrypted-db",
  "author": "carathorys",
  "license": "MIT",
  "preview": true,
  "scripts": {
    "build": "tsc && yarn pack",
    "watch": "tsc --watch",
    "prepublish": "yarn build && yarn tslint",
    "tslint": "tslint src/**/*.ts -t verbose"
  },
  "dependencies": {
    "crypto-js": "^3.1.9-1"
  },
  "devDependencies": {
    "tslint": "^5.7.0",
    "typescript": "^2.5.3"
  }
}
